apiVersion: 1
providers:
  - name: default
    orgId: 1
    folder: ''                  # папка в UI (General)
    type: file
    disableDeletion: false
    allowUiUpdates: true
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards   # путь ВНУТРИ контейнера